{*******************************************************}
{                                                       }
{       Pascal Script Source File                       }
{       Run by RemObjects Pascal Script in CnWizards    }
{                                                       }
{       Generated by CnPack IDE Wizards                 }
{                                                       }
{*******************************************************}

program EditorStatusBar;

uses
  Windows, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs;

var
  Bar: TStatusBar;
begin
  Bar := GetStatusBarFromEditor(GetCurrentEditControl);
  if Bar <> nil then
  begin
    Writeln(Format('Editor StatusBar Found. %d Panels.', [Bar.Panels.Count]));
    Bar.SimplePanel := True;
    Bar.SimpleText := 'CnWizards Pascal Script Example: Change StatusBar Text.';

    // If want back, just change it to False.
    // Bar.SimplePanel := False;
  end;
end.
 
 